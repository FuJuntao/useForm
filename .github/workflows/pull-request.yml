name: Pull Request

on: [push, pull_request]

jobs:
  run-test:
    name: Run Test
    steps:
      - name: Install dependencies
        run: |
          yarn install
      - name: Test
        run: |
          yarn test
