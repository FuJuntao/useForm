name: Pull Request

on: [push, pull_request]

jobs:
  run-test:
    name: Run Test
    runs-on: ubuntu-18.04
    steps:
      - name: Install dependencies
        run: |
          yarn install
      - name: Test
        run: |
          yarn test
