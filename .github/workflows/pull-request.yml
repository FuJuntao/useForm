name: Pull Request

on: [push, pull_request]

jobs:
  run-test:
    name: Run Test
    runs-on: ubuntu-18.04
    steps:
      - use: actions/checkout@actions
      - run: yarn install
      - run: yarn test
